# POC · Topic 图调度最小验证

> 运行：`E:/1shujukuyuanli/anaconda/envs/srtp-memory/python.exe run_poc.py`
> 零网络依赖（离线假 embedding）、零 LLM、不碰 `srtp_memory/` 主链路（只 import 纯函数打分器）。

## 文件

| 文件 | 作用 |
|---|---|
| `fake_embed.py` | 字符 bigram 哈希假向量（同主题 → 余弦偏高），替代 DashScope |
| `graph_gen.py` | 合成 topic 图：9 节点 / 13 边 / DAG 校验 / 90 条记忆 / 6 条带 ground-truth 的查询 |
| `stage_a.py` | 选源五语义：G1 均匀 / G2 单源 / G3 均匀配额 / G4 强度配额 / G4S 软偏置 |
| `run_poc.py` | 五组 × 6 查询跑批 → 4 指标 → 写死判据自动出 go/no-go → 落盘 `poc_results.json` |

## 第一轮结果（合成数据，2026-09-07）

```
group    recall  src_hit  multi_src  compress   kept
G1        0.567    1.000      0.417     0.894    9.5
G2        0.750    0.667      0.000     0.889   10.0
G3        0.592    1.000      0.383     0.889   10.0
G4        0.625    1.000      0.350     0.889   10.0
G4S       0.708    0.917      0.133     0.889   10.0
```

## 第一轮的 4 个结论（比 go/no-go 本身更值钱）

1. **机制全部跑通**：图 → 选源 → 四维打分（复用 `attention.full`）→ 合并去重 → 指标 → 自动判据。
   工程可行性（02 的 Q5）从"纸面确认"升级为"代码验证"。

2. **填充式配额 ≡ 封顶式配额（G4 ≡ G4C，实测完全一致）**。
   数学原因：当 Σquota = K 且每个源池 ≥ quota 时，"全局排序+每源封顶"必然退化为"每源填满配额"。
   → **硬配额只有一种有效语义，想区分就必须用软偏置（G4S）**。03 的消融设计应据此修订。

3. **ground truth 松紧会制造天花板假象**。第一版"任一关键词命中即相关"让相关集（≈15 条）
   超过注入上限 K=10，所有组的 recall 被 `10/|relevant|` 压死，G2 的 0.667 就是天花板而非优势。
   → 正式实验必须报告 **|relevant| 与 recall 天花板**，否则指标不可解释。

4. **合成数据上 G2 赢是构造的产物**：我造数据时让最强边恰好指向最相关节点（强度与相关性完美相关），
   G2 的 0.750 是"作弊"来的；其 src_hit 只有 0.667（漏掉 1/3 的相关源）才是真实代价。
   **判据（G4S vs G2）在合成分布上不可辨识 → 本轮不出 go/no-go，直接进 Q4 真实数据。**

## 值得带进真实数据的一个信号

G4S（软偏置 μ=0.2）在 recall（0.708）与 src_hit（0.917）之间取得了比硬配额（G4：0.625/1.000）
更好的平衡——硬配额为了"源覆盖"牺牲了 12 个点的 recall。真实数据上值得把 μ 扫一遍（0.1/0.2/0.3）。

## 下一步（即 04_poc_plan 的 Step 1 换真实数据）

用 AgentScope 跑"多模块项目"场景自建数据集（Q4 方案 A），保持 `graph_gen.build_graph()` 的输出形状
（nodes / edges / memories / queries + ground truth）不变，`run_poc.py` 无需改动即可复跑。
