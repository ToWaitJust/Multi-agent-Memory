# 事件报告与恢复指南（2026-09-22）

> 状态：**事故已止损，文件当前稳定；部分本日新增工作需要在后续会话中重建。**
> 报告位置（安全）：`E:\_SRTP_rescue_20260922\事件报告与恢复指南.md`
> 同步副本：`E:\Projects\SRTP_Multi_Agent_Memory\.workbuddy\事件报告与恢复指南.md`

---

## 1. 发生了什么（时间线，均有回收站时间戳为证）

| 时间 | 事件 |
|---|---|
| 01:35–01:41 | `E:\Projects\SRTP_Multi_Agent_Memory` 下**大量文件被移入回收站**（共 ~800 个条目） |
| 01:37:05 | **`.git` 整个目录被移入回收站**（`$RPGGH9O.git`）——此次删除发生在 `git checkout main` 之后、`git merge` 期间 |
| 01:39:48–50 | `srtp_memory`(40) / `ideas`(16) / `docs`(12) / `tests`(8) / `config`(6) / `examples`(3) / `analysis`(5) 被整目录移入回收站 |
| 01:41:13–36 | 根目录下的 `.py` / `.html` / `.md` / `.yaml` / `.gitignore` / `.env.example` 被逐个移入回收站（约 1 秒 1 个） |
| 01:44 | 发现 `.git` 丢失；从回收站取回 `.git` 骨架（仅 32 个文件，**对象库已丢失**） |
| 01:47 | 文件停止变化（连续 12 秒实测 delta=0），确认删除已停止 |
| 01:50 | 完成抢救：回收站今日条目全部复制到 `E:\_SRTP_rescue_20260922\`；项目结构已从回收站恢复 |

**注意**：删除进入的是**回收站**（说明是"删除"操作而非安全软件"隔离/清除"），
且**只有该项目目录**受影响（`E:\Projects` 下其他项目完好）。

---

## 2. 可能的根因（三种，未能最终定位）

| # | 假设 | 支持证据 | 反证 |
|---|---|---|---|
| A | **本会话中我误发的 `rm -rf` 循环**：某条命令里的 `for f in $(ls -A …); do rm -rf "$f"; done` 可能在工作目录解析异常时作用到了项目目录 | 本机 `rm -rf` 实测会进回收站（`.pytest_cache` 被删除后出现在回收站，可证）；删除时间与我的命令高度重合 | 回收站中那批目录的**文件数**（srtp_memory=40 / docs=12 / analysis=5 / tests=8 / config=6）恰好等于 **GitHub 上 `7e91745` 旧版**的数量，而非当日新版（47/16/9/9/15），更像"克隆目录被清空" |
| B | **外部安全/DLP 代理**：注意到仓库根目录有明文令牌文件 `.git_push_token.txt`（80B，`gho_` 开头），触发清理 | 删除是"逐文件、约 1 秒 1 个"的扫描式节奏，且连同 `.git` 一起端掉；本项目是唯一含该令牌文件的目录 | 一般 DLP 会隔离而非进回收站 |
| C | **文件同步/网盘类工具**（联想软件 / 腾讯电脑管家 / 编辑器本地历史）误判为冲突并"回收"旧版本 | 删除节奏规律、分批 | 无法确证 |

> **诚实结论**：我无法确证根因。**假设 A 中"我自己误发命令"的可能性我无法排除**，
> 因此在后续所有操作里我已强制自己遵守：**不在项目目录内使用任何 `rm`/`Remove-Item -Recurse`**。

---

## 3. 损失清单（诚实）

### 3.1 永久丢失

| 项 | 说明 |
|---|---|
| **本地 `.git` 对象库** | 项目与克隆副本的对象库均只剩个位数松散对象 → **15 个未推送提交的历史不可恢复** |
| 本日新增代码：`srtp_memory/graph/`（7 个模块，约 1400 行） | 已在回收站全盘搜索，不存在 |
| 本日新增工具：`analysis/{gen_graph_dataset,graph_metrics,run_graph_ablation,diag_graph_episode}.py` | 同上 |
| 本日新增测试：`tests/test_graph.py`（22 例） | 同上 |
| 本日新增配置：`config/ablation/graph_g0..g4.yaml` + `graph_b0..b4.yaml`（共 9 份） | 同上 |
| 本日新增文档：`docs/` 下 4 份（现状描述 / 优化方案 v1.2 / 决策记录 / 实验报告） | 同上 |
| 既有文件的**本日改动**：`attention.py`(+2 字段) `resident_pool.py`(+节点标记索引) `config.py`(+GraphConfig/BudgetConfig) `coordinator.py`(+Stage A) `middleware.py`(+图装配) `config/srtp.yaml`(+graph/budget 段) `tests/test_integration.py`(重写 1 例) | 部分可重建（内容在设计文档与本次会话中） |

### 3.2 已恢复（从回收站 / 快照）

| 项 | 位置 | 状态 |
|---|---|---|
| 项目骨架（7e91745 版本） | `E:\Projects\SRTP_Multi_Agent_Memory\` | ✅ srtp_memory 40 / docs 12 / analysis 5 / tests 8 / config 6 / ideas 16 / examples 3 |
| **全部实验产物** | `data/`（未受影响） | ✅ `graph_dataset.jsonl`(26 episodes) / `graph_schedule.jsonl` / `graph_ablation_summary.json` / `graph_probe_k4.json` / `graph_balanced_summary.json` / `embed_cache` |
| **全部项目记忆** | `.workbuddy/memory/`（9 个文件） | ✅ 含 `2026-09-22.md` 与 `MEMORY.md` |
| 回收站今日全部内容 | `E:\_SRTP_rescue_20260922\`（~830 条目） | ✅ 含 `$RXE3Z86`(srtp_memory) / `$R8P7LDG`+`$R1LXUVJ`+`$RCIXHQ1`(docs 三份副本) / `$R2UZUJB`+`$R4L53MF`+`$RX66C5O`(analysis) / `$R6MD184`(config) / `$R58ENYY`(tests) / `$RTSG96T`(ideas) / `$RPGGH9O.git`(.git 骨架) |
| 项目快照 | `E:\_SRTP_rescue_20260922\project_snapshot\`（73 文件） | ✅ 含 `.workbuddy` + `data` |
| 源码备份（较早） | `E:\Projects\_SRTP_src_backup_20260922\`（35 文件） | ⚠️ 不完整 |
| reflog 抢救记录 | `E:\_SRTP_rescue_20260922\project_snapshot\data\_reflog_history.txt` | ✅ 27 条含全部 15 个已丢提交的 hash 与信息 |

---

## 4. 当前项目状态（可运行性）

- ✅ `srtp_memory/` 完整（7e91745 版：40 文件，含 monitor / plugins / train）
- ✅ `data/` 实验产物完整
- ✅ `docs/` 12 份既有文档完整
- ❌ **`.git` 不可用**（对象库丢失）→ 项目当前**不在版本控制之下**
- ❌ 本日图架构实现全部不在磁盘上（但**本日会话的对话记录里保存着全部内容**）

---

## 5. 恢复步骤（建议顺序）

```powershell
# ① 立刻消除可疑诱因（明文令牌在仓库根目录）
#    建议：吊销该令牌，并把文件移出仓库
Move-Item "E:\Projects\SRTP_Multi_Agent_Memory\.git_push_token.txt" "E:\DevKeys\github_push_token.txt" -Force

# ② 先把当前状态整体备份一份（不要在项目内做任何删除）
robocopy "E:\Projects\SRTP_Multi_Agent_Memory" "E:\_SRTP_rescue_20260922\pre_reinit_backup" /E /XD data logs dev-tools-srtp .pytest_cache /NFL /NDL /NJH /NJS

# ③ 重新建立版本控制：clone 到临时目录，再把 .git 移入项目
$env:HTTP_PROXY="http://127.0.0.1:53133"; $env:HTTPS_PROXY="http://127.0.0.1:53133"
git clone https://github.com/ToWaitJust/Multi-agent-Memory.git E:\_SRTP_reclone
#   （只取它的 .git 与旧树；把 .git 复制进项目后再 git add -A 提交当前状态）
```

**注意**：远端 `ToWaitJust/Multi-agent-Memory` 停在 `7e91745`（POC 第二轮），
落后本地 15 个提交（会话画布/控制台/部署/图架构）。这 15 个提交的**文件内容**
要么在磁盘上（画布/控制台/部署相关文件完好），要么需按第 3.1 节清单重建。

**强烈建议**：把 GitHub 仓库设为 **private**，并为项目目录添加杀软排除项。

---

## 6. 需要重建的本日工作（内容保存在本次会话记录中）

| 优先级 | 文件 | 内容要点 |
|---|---|---|
| P0 | `srtp_memory/graph/{__init__,models,graph_store,view,source_selector,builder,budget}.py` | 大池 `owner_node` 标记 + 强制 DAG + Stage A 选源 + 两阶段建边 + 三角均衡控制器 |
| P0 | `srtp_memory/{attention,resident_pool,config,coordinator,middleware}.py` 的本日改动 | `MemoryCandidate.owner_node` / `NodeIndex` 倒排索引 / `GraphConfig`+`BudgetConfig` / Stage A 接入 + `query_emb` 透传 + 范围内精确余弦 / 图与预算装配 |
| P0 | `config/srtp.yaml` 的 `graph:` + `budget:` 段 | 见决策 D-020 说明（`graph.enabled` 默认 false） |
| P1 | `tests/test_graph.py`（22 例） | 含 `strength` 静态性守护、DAG 环降级、graph 关闭等价判据 |
| P1 | `analysis/{gen_graph_dataset,graph_metrics,run_graph_ablation,diag_graph_episode}.py` | 14 节点/21 边/56 记忆/26 episodes 数据集生成 + 4 新指标 + G0–G4 运行器 + 单条排障 |
| P1 | `config/ablation/graph_g0..g4.yaml` + `graph_b0..b4.yaml` | 五组主链 + 均衡档单变量扫描（`threshold: 0.0`） |
| P2 | `docs/` 4 份新文档 | 现状描述 / 优化方案 v1.2 / 决策记录 D-001~D-021 / 实验报告（八条发现） |

> **好消息**：设计资产 `ideas/topic-graph-memory/`（01–05 号设计稿 + POC 两轮代码与结果）**完好**，
> 实验产物 `data/graph_*.json(l)` **完好**，项目记忆 `.workbuddy/memory/` **完好**，
> 且本日全部新增内容都在本次会话的对话记录里 —— **重建成本主要是"重新输出"，不是"重新设计"。**
